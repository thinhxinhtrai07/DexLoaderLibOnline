<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Quan trọng: Cần quyền Internet để tải file -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:supportsRtl="true">
        
        <!-- 
        Khai báo ContentProvider để tự động chạy khi game khởi động
        KHÔNG CẦN sửa game, chỉ cần nhúng dex và sửa manifest của game
        -->
        <provider
            android:name="com.cakmods.loader.InitProvider"
            android:authorities="${applicationId}.initprovider"
            android:exported="false"
            android:initOrder="9999" />
            
    </application>

</manifest>